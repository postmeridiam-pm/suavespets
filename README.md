pip install pymysql
pip install mysqlclient
pip install djangorestframework


pip install pillow

pip install python-dotenv
pip install social-auth-app-django

pip install bcrypt
pip install django-extensions


pip install Werkzeug
pip install django-axes
pip install django-secure
pip install django-debug-toolbar
pip install django-cachalot

pip install django-axes         
pip install django-secure
pip install django-mysql       
pip install mysqlclient        
pip install django-environ     
pip install django-cors-headers 
pip install django-ratelimit    
pip install --upgrade mysqlclient PyMySQL

pip install social-auth-app-django --break-system-packages

pip install social-auth-core --break-system-packages

python manage.py migrate social_django

pip install google-generativeai

pip install django-dbbackup

pip install python-decouple

pip install python-decouple requests
